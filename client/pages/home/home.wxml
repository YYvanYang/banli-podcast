<!--pages/home/home.wxml-->
<view class="container">
  <view class="page__title">现在收听</view>
  <view>
    <episode-item episode="{{item}}" data-episode="{{item}}" bindtap="goEpisode" wx:for="{{podcasts}}" wx:key="id"></episode-item>
  </view>
</view>